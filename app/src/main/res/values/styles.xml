<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.NoActionBar">
        <!-- Customize your theme here. -->
        <!-- customize the color palette -->
        <item name="colorPrimary">@color/material_blue_grey_800</item>
        <item name="colorPrimaryDark">@color/material_blue_grey_900</item>
        <item name="colorAccent">@color/material_deep_teal_200</item>

        <item name="floatingActionButtonStyle">@style/AppTheme.Fab</item>
    </style>

    <style name="AppTheme.Fab" parent="FloatingActionButton">
        <item name="floatingActionButtonColor">@color/yellow</item>
    </style>

</resources>
